export interface ISuscribe {
  id: number;
  title: string;
  price: number;
  unid: string;
  describe: string;
}

export interface ICreatePlot {
  id: number;
  surface: string; 
  cereal: string;  
  user: string;
}

